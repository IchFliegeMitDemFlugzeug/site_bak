# Исходные фотографии сохранены у владельца проекта и не нужны в опубликованной сборке.
Photo/

# Готовая папка создаётся заново при каждой сборке сайта.
dist/

# Локально установленные зависимости восстанавливаются из package-lock.json.
node_modules/
