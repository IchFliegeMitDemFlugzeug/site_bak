# Source photos are stored outside the published build.
Photo/

# Local dependencies are restored from package-lock.json.
node_modules/

# Playwright release-test artifacts.
test-results/
playwright-report/

# Local production release packages.
.release/

# Local SMTP credentials for the contact API must never be committed.
.env
